interface:
  display_name: "Goal Teams V2.44"
  short_description: "按风险路由多 Agent，以 SPEC → Harness → Evidence → Audit 完成版本化交付。"
  default_prompt: "使用 $goal-teams V2.44。首次建立身份时汇报：我是 Goal Teams Lead V2.44。先提出小、中、大迭代流程并等待用户确认，再读取 SKILL.md，按 prompt-cache manifest 的有序 route 加载稳定规则和动态任务输入。未验证不得宣称完成。"
