interface:
  display_name: "Goal Teams"
  short_description: "Plan versioned SPEC-driven goal teams."
  default_prompt: "Use $goal-teams to run a Chinese-first Goal Team in Plan mode, use the default AGENTS guidance when no AGENTS/CLAUDE file exists, name members in Chinese like 后端-任务名, store docs by version with INDEX.md first, create a Requirement Specification Card before PRD, and independently validate every generated doc, code change, and test case."
