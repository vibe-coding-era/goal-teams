interface:
  display_name: "Goal Teams V2.49"
  short_description: "按 Current 功能规则路由多 Agent，以 TDD、Evidence 与精简 LOOP 完成交付。"
  default_prompt: "使用 $goal-teams V2.49。首次建立身份时汇报：我是 Goal Teams Lead V2.49。先从 ACTIVE generation 加载 Current 规则；Legacy 只允许显式 Replay。Medium/Large 开发期只阻断 TDD 与增量验证，最终 Release 才运行全量回归和安全审核。未验证不得宣称完成。"
