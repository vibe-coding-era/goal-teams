name: Goal Teams checks

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  check:
    name: V2.3 release gates (${{ matrix.os }})
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4.3.1
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5.6.0
        with:
          python-version: '3.11'
      - name: Structure, schema, negative, canonical, security, and context gates
        env:
          GOAL_TEAMS_INSTALL_VALIDATION: '1'
          PYTHONDONTWRITEBYTECODE: '1'
        run: ./scripts/check.sh
      - name: Installer lifecycle and transaction fault injection
        env:
          PYTHONDONTWRITEBYTECODE: '1'
        run: python3 scripts/checks/check-install-lifecycle.py
