# Goal Teams V2.66 Release

V2.66 is the current published product release. GitHub Release `377935171`, the annotated `v2.66` tag, the fixed four public assets, exact remote readback, and the formal canonical local installation have completed. The installed package contains 343 package files and 24 agent files with zero symlinks.

V2.66 adds the digest-bound Goal-Teams execution dashboard and hardens the formal self-release path while retaining the V2.5 portable execution core, the V2.65 Graph execution assets, v2.50 release controls, and the V2.3 Legacy data schema.

## V2.66 changes

- Adds the `Goal-Teams 任务执行看板`, `Context / Knowledge / Tools`, and PDCA-aligned LOOP output contract with active/remaining task projection and linked TaskList evidence.
- Binds Current output rules, schema, implementation, tests and generated members into the V2.66 activation closure.
- Adds V2.66-specific released-runtime, release-control, repository-boundary and S4 continuation contracts while keeping the reusable v2.50 execution core.
- Runs the exact Current full-regression denominator and a separate-process release security review before the single S2 build.
- Uses one same-asset S2 build, independent repository-boundary checks, a Large-only S3 install lifecycle, and checkpoint-bound S4 continuation without rebuilding.
- Keeps V2.65 as the published predecessor and excludes predecessor/Legacy test roots from the V2.66 Current execution denominator.

## Compatibility retained from V2.6

- Added a fail-closed compatibility chain: `Portable Core -> Host -> Provider -> Model -> optional Bridge -> Role -> Task`.
- Added explicit Codex and Claude Code host overlays, DeepSeek provider metadata, and Kimi K3 model routing.
- Added deterministic thin role projections for Codex TOML and Claude Code Markdown, including missing, drift, and managed-orphan checks.
- Kept provider and model runtime claims unverified unless a trusted runtime-binding receipt exists; structural compatibility does not imply live provider success.
- Kept Legacy out of Current routing and the default package unless an explicit trusted Replay request is supplied.

## Public assets

The fixed public set is:

- `goal-teams-V2.66.tar.gz` — 1,723,338 bytes — SHA-256 `51e6adc7bf99880ebad73a2e1c974d06aebc4a5831c59dcb6ce3418a029617e1`
- `SHA256SUMS` — 90 bytes — SHA-256 `d7562f5010a24b9ca961d7363f7abab85ffe1b94c98e77049a036edb06b4b298`
- `_release.json` — 1,761 bytes — SHA-256 `1903f2b860b4ce872893fb337ed58f13afc39cd34c6b00dbcabb6156f271ee78`
- `_files.sha256` — 41,353 bytes — SHA-256 `336fb4b72361f27f42e90e57d5ca46a0266faf41424db70ab6414c89e2dcc321`

No `docs/`, `develops/`, local Evidence, credentials, or optional Replay supplement is part of the default asset.

## Projection boundary

This file is the post-release `main` projection of the verified live V2.66 Release. The immutable V2.66 assets retain the candidate-time `release/current` projection captured by the single S2 build and are not rewritten after publication. The immutable V2.65 assets retain the candidate-time projection from their own release and remain untouched by this V2.66 projection.

## Completion telemetry

- Tokens consumed: Unavailable / 未获取到
- Cache hit rate: Unavailable / 未获取到

## Assurance boundary

A fresh runtime transition receipt proves only an I1 correlated process observation. The installed package, local Graph Runtime tests and canonical installation do not prove repository-external independence, provider final-prompt assembly, real business execution or business acceptance. Tokens consumed and cache hit rate remain unavailable unless trusted host usage Evidence exists.

Requirements: Python 3.11+ for the complete validated toolchain.
